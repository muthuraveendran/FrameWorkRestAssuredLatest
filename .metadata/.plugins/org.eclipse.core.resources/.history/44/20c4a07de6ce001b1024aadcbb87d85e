package TestSuite;

import static org.testng.Assert.assertEquals;

import org.testng.annotations.Test;

import helper.NeedleNineUtils;
import io.restassured.response.Response;
import utils.Constants;

public class StatusCode {
	NeedleNineUtils needleNineUtils = new NeedleNineUtils();
	
	@Test
	public void loginPage() {
		Response response = needleNineUtils.checkStatusCode(Constants.login);
		assertEquals(response, false);
 	};
 	
	@Test
 	public void AfterloginPage() {
		needleNineUtils.checkAuthStatusCode(Constants.loginData);
 	};
 	
 	@Test
 	public void coursePage() {
		needleNineUtils.getStatus(Constants.course);
 	};
 	
 	@Test
 	public void schedulePage() {
		needleNineUtils.getStatus(Constants.schedule);
 	};
 	
 	
};
